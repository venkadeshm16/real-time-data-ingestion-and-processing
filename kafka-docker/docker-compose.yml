version: '3.8'

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    container_name: zookeeper
    restart: unless-stopped
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
      ZOOKEEPER_INIT_LIMIT: 5
      ZOOKEEPER_SYNC_LIMIT: 2
      KAFKA_JMX_HOSTNAME: zookeeper
      KAFKA_JMX_PORT: 9999
    ports:
      - "2181:2181"
      - "9999:9999"
    volumes:
      - zookeeper-data:/var/lib/zookeeper/data
      - zookeeper-logs:/var/lib/zookeeper/log
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    networks:
      - icu-network

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: kafka
    restart: unless-stopped
    depends_on:
      zookeeper:
        condition: service_healthy
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:9093
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'false'
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_LOG_SEGMENT_BYTES: 1073741824
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_JMX_HOSTNAME: kafka
      KAFKA_JMX_PORT: 9998
    ports:
      - "9092:9092"
      - "9093:9093"
      - "9998:9998"
    volumes:
      - kafka-data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
    networks:
      - icu-network

  kafka-jmx-exporter:
    image: prom/jmx-exporter:0.20.0
    container_name: kafka-jmx-exporter
    restart: unless-stopped
    ports:
      - "8080:8080"
    command:
      - "8080"
      - "/etc/jmx_exporter/kafka.yml"
    volumes:
      - ./jmx-config/kafka.yml:/etc/jmx_exporter/kafka.yml
    depends_on:
      - kafka
    networks:
      - icu-network

  zookeeper-jmx-exporter:
    image: prom/jmx-exporter:0.20.0
    container_name: zookeeper-jmx-exporter
    restart: unless-stopped
    ports:
      - "8081:8080"
    command:
      - "8080"
      - "/etc/jmx_exporter/zookeeper.yml"
    volumes:
      - ./jmx-config/zookeeper.yml:/etc/jmx_exporter/zookeeper.yml
    depends_on:
      - zookeeper
    networks:
      - icu-network

volumes:
  zookeeper-data:
  zookeeper-logs:
  kafka-data:

networks:
  icu-network:
    external: true
